services:
  mongo:
    image: mongo
    networks:
      - default
  backend:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - mongo
    networks:
      - default

networks:
  default:
