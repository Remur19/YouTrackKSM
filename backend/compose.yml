services:
  mongo:
    image: mongo
    networks:
      - default
  backend:
    build: .
    ports:
      - "27017:27017"
    depends_on:
      - mongo
    networks:
      - default

networks:
  default:
